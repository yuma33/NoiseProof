<%= turbo_stream.replace "like-button-for-post-#{@post.id}" do %>
  <%= render 'posts/unlike', post: @post %>
<% end %>

<%= turbo_stream.replace "likes-count-#{@post.id}" do %>
  <%= render 'posts/like_count', post: @post %>
<% end %>